{
  "name": "samsinn",
  "version": "0.5.5",
  "description": "Multi-agent room communication system with delivery modes, flows, muting, directed addressing, and Markdown rendering",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/main.ts",
    "dev": "bun --watch src/main.ts",
    "check": "tsc --noEmit",
    "test": "bun test",
    "test:unit": "bun test --filter '!Ollama'"
  },
  "keywords": ["agents", "multi-agent", "llm", "ollama", "communication", "rooms", "bun", "typescript"],
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "1.3.11"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1"
  }
}
